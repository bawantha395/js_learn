document.querySelector("h1").innerText ="New Heading";
console.log("script loaded");

// Defer and Async  attributes =>
// defer use to execute external .js scripts/files in header section. Then It don't need add to bottom of the body part. If interact with DOM
// async use to execute external .js scripts/files. It doesn't wait to execute content it body. ( first come first serve ) If doesn’t interact with DOM
